---
interface Props {
  title?: string
  center?: boolean
}

const { title, center = false } = Astro.props
---

<section class="max-w-3xl mx-auto mb-12">
  {title && <h2 class={`text-2xl font-black mb-5 ${center ? 'text-center' : ''}`}>{title}</h2>}
  <slot />
</section>
